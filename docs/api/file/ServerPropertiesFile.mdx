---
name: ServerPropertiesFile class
route: /api/file/ServerPropertiesFile
menu: Instance File APIs
---

# `ServerPropertiesFile` class

Represents a [`server.properties`][server.properties] file generated by a Minecraft server instance.

This class extends [`TextFile`](./TextFile).

## File Format

The properties file stores a set of key/value pairs, each representing an configuration option of the server instance.

Typically, it will look something like this:

```txt
#Minecraft server properties
#2018-10-23T14:22:01
generator-settings=
op-permission-level=4
allow-nether=true
enforce-whitelist=false
level-name=world
enable-query=false
allow-flight=false
prevent-proxy-connections=false
server-port=25565
max-world-size=29999984
level-type=DEFAULT
enable-rcon=false
level-seed=
force-gamemode=false
server-ip=
network-compression-threshold=256
max-build-height=256
spawn-npcs=true
white-list=false
spawn-animals=true
hardcore=false
# and more!
```

## Static Methods

### `parseValue`

Takes a serialized value from a property key/value pair, and makes a fairly intelligent attempt at coercing it to a boolean, number or a string.

### `serializeValue`

The inverse of `parseValue` -- takes a number, boolean or string, and converts it to a string representation.

## Instance Methods

### `get`

Reads a single property value from the file using its key.

#### Parameters

1. `key: string`

   The key to select.

#### Return Value

Returns a Promise that resolves with the value from the file.

### `set`

Writes a single property value to the file using its key and new value.

#### Parameters

1. `key: string`

   The key to update.

2. `value: number | string | boolean | null`

   The new value to set for the specified key.

#### Return Value

Returns a Promise that resolves with the value from the file.

[server.properties]: https://minecraft.gamepedia.com/Server.properties
